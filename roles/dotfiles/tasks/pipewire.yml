---
- name: Pipewire
  tags: [pipewire]
  block:
    - name: Install pipewire
      community.general.xbps:
        update_cache: false
        name:
          - pipewire
          - wireplumber
          - alsa-pipewire
          - libspa-bluetooth
          - fdk-aac  # AAC codec for Bluetooth
          - pavucontrol  # GTK GUI for audio control (works via pipewire-pulse)
          - easyeffects  # PipeWire audio effects (EQ, compressor, limiter)
        state: present
      become: true

    - name: Create WirePlumber 0.5 config directory
      ansible.builtin.file:
        path: "{{ user_home }}/.config/wireplumber/wireplumber.conf.d"
        state: directory
        mode: "0755"

    - name: Remove old deprecated Lua config directories
      ansible.builtin.file:
        path: "{{ user_home }}/.config/wireplumber/{{ item }}"
        state: absent
      loop:
        - bluetooth.lua.d
        - main.lua.d
        - policy.lua.d

    - name: Configure Bluetooth AAC codec and Bose QC45
      ansible.builtin.copy:
        content: |
          # Bluetooth configuration for WirePlumber 0.5+
          # AAC codec preference and Bose QC45 settings

          monitor.bluez.properties = {
            bluez5.enable-sbc-xq = true
            bluez5.enable-msbc = true
            bluez5.enable-hw-volume = true
            bluez5.headset-roles = [ hsp_hs hsp_ag hfp_hf hfp_ag ]
            bluez5.codecs = [ aac sbc_xq sbc ldac aptx aptx_hd ]
            bluez5.auto-connect = [ hfp_hf hsp_hs a2dp_sink ]
          }

          # Bose QC45 specific rule - force A2DP sink profile
          monitor.bluez.rules = [
            {
              matches = [
                {
                  device.name = "bluez_card.{{ bose_mac | replace(':', '_') }}"
                }
              ]
              actions = {
                update-props = {
                  device.profile = "a2dp-sink"
                }
              }
            }
          ]
        dest: "{{ user_home }}/.config/wireplumber/wireplumber.conf.d/50-bluez-config.conf"
        mode: "0644"

    - name: Configure stream switching policy
      ansible.builtin.copy:
        src: pipewire/50-policy.conf
        dest: "{{ user_home }}/.config/wireplumber/wireplumber.conf.d/50-policy.conf"
        mode: "0644"

    - name: Limit volume to 100% (no over-amplification)
      ansible.builtin.copy:
        src: pipewire/50-volume-limit.conf
        dest: "{{ user_home }}/.config/wireplumber/wireplumber.conf.d/50-volume-limit.conf"
        mode: "0644"

    - name: Create EasyEffects output preset directory
      ansible.builtin.file:
        path: "{{ user_home }}/.config/easyeffects/output"
        state: directory
        mode: "0755"

    - name: Deploy EasyEffects presets
      ansible.builtin.copy:
        src: easyeffects/output/
        dest: "{{ user_home }}/.config/easyeffects/output/"
        mode: "0644"
