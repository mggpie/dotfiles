---
- name: Install newsboat
  community.general.xbps:
    name: newsboat
    state: present
  become: true
  tags: [newsboat]

- name: Create newsboat config directory
  ansible.builtin.file:
    path: "{{ user_home }}/.config/newsboat"
    state: directory
    mode: "0755"
  tags: [newsboat]

- name: Create newsboat home directory
  ansible.builtin.file:
    path: "{{ user_home }}/.newsboat"
    state: directory
    mode: "0755"
  tags: [newsboat]

- name: Deploy newsboat config
  ansible.builtin.copy:
    src: newsboat/config
    dest: "{{ user_home }}/.config/newsboat/config"
    mode: "0644"
  tags: [newsboat]

- name: Deploy newsboat urls
  ansible.builtin.copy:
    src: newsboat/urls
    dest: "{{ user_home }}/.config/newsboat/urls"
    mode: "0644"
  tags: [newsboat]

- name: Deploy newsboat bookmark script
  ansible.builtin.copy:
    src: newsboat/bookmark.sh
    dest: "{{ user_home }}/.newsboat/bookmark.sh"
    mode: "0755"
  tags: [newsboat]
