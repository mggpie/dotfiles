---
- name: Restart bluetoothd
  ansible.builtin.command: sv restart bluetoothd
  changed_when: true
  become: true

- name: Regenerate GRUB config
  ansible.builtin.command: grub-mkconfig -o /boot/grub/grub.cfg
  changed_when: true
  become: true

- name: Apply sysctl settings
  ansible.builtin.command: sysctl --system
  changed_when: true
  become: true

# solaar overwrites config.yaml on exit, so we must kill it before deploy
# and restart it explicitly - sway's exec only runs once at login
- name: Restart solaar
  ansible.builtin.shell: |
    pkill -f solaar || true
    sleep 1
    nohup solaar --window=hide >/dev/null 2>&1 &
  changed_when: true
