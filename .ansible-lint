# .ansible-lint configuration
---
profile: basic

# Skip these rules
skip_list:
  - var-naming[no-role-prefix]  # We don't want to prefix all variables
  - no-handler                  # Sometimes we need to run tasks on change inline
  - command-instead-of-module   # curl | sh is intentional for Nix install
  - risky-shell-pipe            # We handle pipe failures manually

# Paths to exclude
exclude_paths:
  - .git/
  - docs/
  - installer/

# Warn on these rules instead of failing
warn_list:
  - yaml[line-length]
  - name[casing]
